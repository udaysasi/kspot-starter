import { Routes } from '@angular/router';
import { SitePickerComponent } from './site-picker.component';

export default [
    {
        path: '',
        component: SitePickerComponent,
    },
] as Routes;